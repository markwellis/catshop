class local_config {
    notify{'Local config defined. Including it.':}

    #
    # If you want to include you're own puppet config fore the development 
    # environment then copy this example module to local_config, the whole 
    # folder, then edit this file in the copy to include your own copy.
    #
}
